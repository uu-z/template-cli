{
  "name": "mycli",
  "version": "0.0.1",
  "main": "dist/index.js",
  "bin": "bin/index.js",
  "files": ["dist", "bin"],
  "license": "MIT",
  "scripts": {
    "build": "rm -rf ./dist; babel ./src --out-dir dist",
    "dev": "yarn run build; npm link",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "menhera": "^0.6.7-rc2",
    "menhera-cli": "^0.2.9-rc1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.6.1"
  }
}
